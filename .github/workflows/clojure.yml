name: Clojure CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: clojure

    steps:
    - uses: actions/checkout@v4
    - name: Install dependencies
      run: clj -M:dev uberjar
